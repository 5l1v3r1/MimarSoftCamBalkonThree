@model UI.WebMvcCore.Models.ViewModels.ErrorViewModel
@{ ViewData["Title"] = "Error"; }

<h1 class="text-danger">Hata.</h1>
<h2 class="text-danger">İsteğiniz işlenirken bir hata oluştu.</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>Request ID:</strong> <code>@Model.RequestId</code>
    </p>}

<h3>Geliştirme Modu</h3>
<p>
    <strong>Geliştirme</strong> ortamına geçiş, oluşan hata hakkında daha ayrıntılı bilgi görüntüler.
</p>
<p>
    <strong>Geliştirme ortamı, dağıtılan uygulamalar için etkinleştirilmemelidir.</strong>
    İstisnalardan son kullanıcılara kadar hassas bilgilerin görüntülenmesine neden olabilir.
    Yerel hata ayıklama için, <strong>ASPNETCORE_ENVIRONMENT</strong> ortam değişkenini <strong>Geliştirme</strong> olarak ayarlayarak <strong>Geliştirme</strong> ortamını etkinleştirin
    ve uygulamayı yeniden başlatma.
</p>

